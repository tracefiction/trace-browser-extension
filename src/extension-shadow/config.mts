// This placeholder is replaced only while assembling the disposable Chrome
// shadow build. The module is never copied into normal dev or release output.
export const TRACE_SHADOW_API_BASE = "__TRACE_API_BASE__";
